import React, { Component } from 'react';
import { Link } from 'react-router';

export class PreBooksList extends Component {
	
	static propTypes = {
		books: React.PropTypes.array
	};

	render() {
		let { books } = this.props;
		return (
			
		);
	}
}